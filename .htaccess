RewriteEngine on
# RewriteBase /
# Rewrites all URLS without [issues] in them
# RewriteCond %{REQUEST_URI} !^/issues/
# RewriteRule ^([^/]*)$ index.php?p=$1 [L]
# RewriteRule ^index\.html?$ / [NC,R,L]
